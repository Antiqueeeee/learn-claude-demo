# AGENTS.md Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Update the root `AGENTS.md` so coding agents have accurate commands, style guidance, and security rules for this repository.

**Architecture:** Modify the existing root `AGENTS.md` in place. Base every instruction on files that actually exist in the repo, and explicitly document the absence of Cursor/Copilot rules and the rule against reading `.env` contents by default.

**Tech Stack:** Markdown documentation, Python 3.13 codebase, unittest-compatible test invocation, OpenAI SDK streaming loop.

---

### Task 1: Reconfirm repository facts used by the document

**Files:**
- Read: `AGENTS.md`
- Read: `main.py`
- Read: `tooling.py`
- Read: `modes/the_agent_loop.py`
- Read: `.gitignore`

**Step 1: Verify current commands**

- Confirm the repo can be run with `python main.py`.
- Confirm syntax-check guidance should use `python -m compileall -q .`.
- Confirm there is no test suite checked in and no lint config file.

**Step 2: Verify secret-handling guidance**

- Confirm `.env` is gitignored.
- Confirm `.env.example` exists for non-secret variable references.

### Task 2: Rewrite the root AGENTS.md

**Files:**
- Modify: `AGENTS.md`

**Step 1: Replace generic phrasing with repo-specific instructions**

- Keep the document focused on this repository's actual structure and workflows.

**Step 2: Add accurate command guidance**

- Include run/build/test commands that work now.
- Include a single-test section using `python -m unittest -v tests.test_module.TestClass.test_name`.
- State clearly when a tool is not configured in-repo.

**Step 3: Strengthen code-style rules**

- Document import grouping, 4-space indentation, type-hint expectations, naming conventions, and error-handling style inferred from existing code.

**Step 4: Add security constraints**

- State that agents must not read `.env` contents unless the user explicitly requests it.
- Direct agents to `.env.example` and `engines/configEngine.py` for variable names.
- State that secrets must never appear in outputs, logs, docs, or commits.

**Step 5: Record external rule status**

- State that no Cursor rules or Copilot instructions were found.

### Task 3: Verify the updated document

**Files:**
- Read: `AGENTS.md`

**Step 1: Check length and coverage**

- Ensure the file is approximately 150 lines long.
- Ensure it contains build/lint/test guidance, single-test guidance, style rules, and security guidance.

**Step 2: Check wording accuracy**

- Confirm the document does not claim the repo uses tools that are not configured.
- Confirm the `.env` rule is explicit and easy for agents to follow.

### Task 4: Final validation

**Files:**
- Read: `AGENTS.md`

**Step 1: Sanity-check for agent usefulness**

- Confirm a new coding agent could use the file without additional repo context.

**Step 2: Keep changes scoped**

- Do not modify application code.
- Do not read `.env` contents during the work.
